import React from 'react';

import Sidebar from '../../features/sidebar';

import Wrapper from '../../shared/ui/wrapper';

const Main = () => {
  return (
    <div>
      <Sidebar />
    </div>
  );
};

export default Main;
